# Meeting Scheduler — environment configuration

# Database URL (SQLite for development, Postgres for production)
DATABASE_URL=sqlite+aiosqlite:///./meetings.db

# LLM API key (only needed for Level B — ai_layer)
# LLM_API_KEY=sk-...